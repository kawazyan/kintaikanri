"use client";import {useTransition} from "react";import {useRouter} from "next/navigation";import {createInvoiceDraft} from "./actions";
export function CreateInvoiceButton({clients}:{clients:{id:string,name:string}[]}){const [pending,start]=useTransition();const r=useRouter();return <form action={(fd)=>start(async()=>{const id=await createInvoiceDraft(fd);r.push(`/admin/invoices/${id}`)})} className="grid gap-3 rounded-2xl border border-slate-700 bg-slate-900 p-5 md:grid-cols-[1fr_180px_auto]"><select name="clientId" required className="rounded-xl bg-slate-800 px-3 py-2"><option value="">取引先を選択</option>{clients.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><input type="month" name="yearMonth" required className="rounded-xl bg-slate-800 px-3 py-2"/><button disabled={pending} className="rounded-xl bg-red-600 px-5 py-2 font-black">{pending?"作成中…":"請求下書きを作成"}</button></form>}
