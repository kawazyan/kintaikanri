import Link from "next/link";import {requireAdmin} from "@/lib/auth";import {prisma} from "@/lib/prisma";import {AdminNav} from "../admin-nav";import {CreateInvoiceButton} from "./create-button";
export default async function InvoicesPage(){await requireAdmin();const [clients,invoices]=await Promise.all([prisma.client.findMany({where:{active:true},orderBy:{name:"asc"}}),prisma.invoice.findMany({include:{client:true},orderBy:{createdAt:"desc"},take:100})]);return <main className="mx-auto max-w-5xl px-4 py-8 text-slate-100"><AdminNav/><h1 className="text-2xl font-black">請求管理</h1><div className="mt-5"><CreateInvoiceButton clients={clients.map(c=>({id:c.id,name:c.name}))}/></div><div className="mt-6 space-y-3">{invoices.map(i=><Link key={i.id} href={`/admin/invoices/${i.id}`} className="flex flex-wrap items-center justify-between gap-3 rounded-2xl border border-slate-700 bg-slate-900 p-5"><div><p className="font-black">{i.client.name} ・ {i.yearMonth}</p><p className="text-xs text-slate-400">{i.invoiceNumber} ・ {i.status}</p></div><p className="text-lg font-black">¥{i.totalInclTax.toLocaleString()}</p></Link>)}</div></main>}
