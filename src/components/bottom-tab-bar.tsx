"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { Home, History, CalendarDays, Bell, Menu, type LucideIcon } from "lucide-react";
const TABS:{href:string;label:string;icon:LucideIcon}[]=[{href:"/clock",label:"ホーム",icon:Home},{href:"/history",label:"打刻履歴",icon:History},{href:"/shift",label:"シフト",icon:CalendarDays},{href:"/notices",label:"お知らせ",icon:Bell},{href:"/menu",label:"メニュー",icon:Menu}];
export function BottomTabBar(){const pathname=usePathname();return <nav className="fixed inset-x-0 bottom-0 z-50"><div className="mx-auto max-w-[430px] px-4 pb-[calc(.5rem+env(safe-area-inset-bottom))]"><div className="flex items-stretch justify-between overflow-hidden rounded-[24px] border border-slate-200/80 bg-white/95 px-2 py-1.5 shadow-[0_-2px_8px_rgba(15,23,42,.03),0_10px_28px_rgba(15,23,42,.12)] backdrop-blur-xl">{TABS.map(({href,label,icon:Icon})=>{const active=pathname===href||(href!=="/clock"&&pathname.startsWith(`${href}/`));const cls=`relative flex min-h-[58px] flex-1 flex-col items-center justify-center gap-1 rounded-xl text-[10px] font-bold transition active:scale-95 ${active?"text-red-600":"text-slate-600"}`;if(href==="/clock")return <button key={href} type="button" className={cls} onClick={()=>window.location.assign("/clock")}><Icon size={24} strokeWidth={active?2.7:2.1}/><span>{label}</span>{active&&<span className="absolute inset-x-3 bottom-0 h-[3px] rounded-full bg-red-600"/>}</button>;return <Link key={href} href={href} className={cls}><Icon size={24} strokeWidth={active?2.7:2.1}/><span>{label}</span>{active&&<span className="absolute inset-x-3 bottom-0 h-[3px] rounded-full bg-red-600"/>}</Link>})}</div></div></nav>}
