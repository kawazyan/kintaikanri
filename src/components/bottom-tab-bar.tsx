"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { Home, History, CalendarDays, Bell, Menu, type LucideIcon } from "lucide-react";
const TABS:{href:string;label:string;icon:LucideIcon}[]=[{href:"/clock",label:"ホーム",icon:Home},{href:"/history",label:"打刻履歴",icon:History},{href:"/shift",label:"シフト",icon:CalendarDays},{href:"/notices",label:"お知らせ",icon:Bell},{href:"/menu",label:"メニュー",icon:Menu}];
export function BottomTabBar(){const pathname=usePathname();return <nav className="fixed inset-x-0 bottom-0 z-50"><div className="mx-auto max-w-[430px] px-3 pb-[calc(.45rem+env(safe-area-inset-bottom))]"><div className="flex items-stretch justify-between overflow-hidden rounded-[22px] border border-white/10 bg-gradient-to-b from-[#17283a]/[.98] to-[#0b1521]/[.98] px-1.5 py-1.5 shadow-[0_8px_28px_rgba(2,8,23,.28),inset_0_1px_0_rgba(255,255,255,.10)] backdrop-blur-xl">{TABS.map(({href,label,icon:Icon})=>{const active=pathname===href||(href!=="/clock"&&pathname.startsWith(`${href}/`));const cls=`relative flex min-h-[58px] flex-1 flex-col items-center justify-center gap-1 rounded-[15px] text-[9px] font-black transition active:scale-95 ${active?"bg-white/10 text-white shadow-[inset_0_1px_0_rgba(255,255,255,.10)]":"text-slate-400"}`;const inner=<><Icon size={22} strokeWidth={active?2.7:2.1}/><span className="whitespace-nowrap">{label}</span>{active&&<span className="absolute inset-x-5 bottom-0 h-[3px] rounded-full bg-red-500 shadow-[0_0_10px_rgba(239,68,68,.7)]"/>}</>;if(href==="/clock")return <button key={href} type="button" className={cls} onClick={()=>window.location.assign("/clock")}>{inner}</button>;return <Link key={href} href={href} className={cls}>{inner}</Link>})}</div></div></nav>}
